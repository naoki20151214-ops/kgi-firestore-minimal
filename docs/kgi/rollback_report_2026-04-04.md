# KGI作成フロー 安定化ロールバック報告（2026-04-04）

## 1. 方針（安全な revert ベース）
- main 履歴を壊す reset / force push は行わない。
- PR #220 / #221 の改善を残し、PR #222 / #223 / #224 だけを戻す。
- 競合時は「最初の2問 -> AI追加質問 -> KGI元データ確認 -> 保存」の流れを優先して解消する。

## 2. 調査結果
### 2-1. 対象PRの実体コミット
- PR #222: `bc36b00`（KGI面談を1ターン統合API中心に再構成）
- PR #223: `b2e5378`（run_turn_api_error 対応）
- PR #224: `84718cb`（既存APIへの統合変更）

### 2-2. 現在HEADの状態確認
- HEAD には PR #226 (`ae24d59`) が入っており、PR #222 の変更は revert 済み。
- `git diff --name-status 363880c..HEAD` の結果が空で、
  PR #221 時点の tree と HEAD の tree が一致。
- つまり、PR #223 / #224 由来の変更も net では残っていない（run-kgi-interview-turn API追加も消滅）。

## 3. 何を戻し、何を残したか
### 戻っている（=外れている）
- AI一貫ターン方式への大規模変更（#222）
- run_turn_api_error 対応で追加された新API系（#223）
- その後の既存API統合変更（#224）

### 残っている（#220/#221）
- 質問文自然化の改善
- セッション混線防止の仕組み（sessionId / fingerprint / draft復元系）
- 保存不可時の不足質問へ戻す導線

## 4. 変更ファイル一覧
- この確認結果を記録するために以下のみ追加。
  - `docs/kgi/rollback_report_2026-04-04.md`

## 5. 最低限の動作確認手順（手動）
1. KGI作成画面を開く。
2. 最初の2問（期限 / 成功状態）を入力して「次へ」。
3. 追加質問が1問ずつ表示されることを確認。
4. 情報不足状態でKGI元データ作成/保存に進み、不足質問への復帰導線が出ることを確認。
5. ブラウザ再読込後に同一セッション草案が復元されることを確認（混線しないこと）。

## 6. 残リスク
- このリポジトリ単体ではE2E自動テストがないため、UIフロー保証は手動確認依存。
- APIキー未設定環境ではAI応答の実挙動を完全再現できない。
- 将来の追加PRで同系統の統合ロジックが再混入する可能性があるため、
  #220/#221 相当をベースラインとして差分監視が必要。
